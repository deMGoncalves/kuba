/**
 * Converte os caracteres de maiusculas para minusculas
 *
 * @name toLower
 * @function
 * @access private
 * @param {String} target Caractes a ser convertido
 * @returns {String} Caracteres convertido para maiusculas
 */
export default (target) =>
  target.toLowerCase()
