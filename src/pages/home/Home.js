import { paint } from '@h'
import metadata from '@metadata'
import opengraph from '@opengraph'
import component from './component'

@paint(component)
@metadata
@opengraph
class Home {
}

export default Home
