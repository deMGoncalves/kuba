import styled from '@kuba/styled'

export default styled`
  .language__container {
    display: flex;
    flex-direction: column;
    gap: var(--spacing_inset-nano);
    width: 100%;
  }

  .language {
    display: flex;
    flex-direction: column;
    gap: var(--spacing_inset-nano);
  }
`
