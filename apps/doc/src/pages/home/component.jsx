import h from '@rex/h'
import style from './style.css'

export default (home) =>
  <main className={style.home}>
    {home.navigation}
    <section className={style.home__content}>
      blau
    </section>
  </main>
