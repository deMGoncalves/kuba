import styled from '@kuba/styled'

export default styled.style`
  .modelo {
    line-height: var(--line-height-sm) !important;
    text-align: right;
  }

  .modelo + .modelo {
    text-align: left;
  }
`
