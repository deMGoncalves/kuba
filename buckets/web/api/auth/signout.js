/*
import supabase from '@kuba/supabase'

export default async function (request, response) {
  const { authorization } = request.headers
  const { error } = await supabase.auth.api.signOut(authorization)

  response.json({ error })
}
*/
