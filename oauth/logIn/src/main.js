import styled from '@kuba/styled'

export default styled.main`
  display: flex;
  flex-direction: column;
  gap: var(--spacing_inset-lg);
  height: 100vh;
  justify-content: center;
  margin: 0 auto;
  max-width: 408px;
  padding: var(--spacing_inset-md) var(--spacing_inset-xs);
`
