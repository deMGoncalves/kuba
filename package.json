{
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^5.1.1",
    "lint-staged": "^11.1.2",
    "pinst": "^2.1.4"
  },
  "engines": {
    "node": "14.x"
  },
  "name": "kuba",
  "private": true,
  "scripts": {
    "heroku-postbuild": "yarn workspace @kuba/ecomm build",
    "postinstall": "husky install",
    "test": "yarn workspaces run test"
  },
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "actions/*",
      "blocks/*",
      "buckets/*",
      "directives/*",
      "development/*",
      "elements/*",
      "filters/*",
      "infra/*",
      "models/*",
      "pages/*",
      "pixels/*",
      "shared/*"
    ]
  }
}
