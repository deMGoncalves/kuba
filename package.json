{
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^5.1.1",
    "pinst": "^2.1.4"
  },
  "name": "kuba",
  "private": true,
  "scripts": {
    "postinstall": "husky install",
    "test": "yarn workspaces -p run test"
  },
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "actions/*",
      "blocks/*",
      "buckets/*",
      "dev/*",
      "elements/*",
      "filters/*",
      "infra/*",
      "models/*",
      "pages/*",
      "pixels/*",
      "shared/*"
    ]
  }
}
