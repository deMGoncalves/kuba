describe('standard.h.className', () => {
  test.todo('Atributo key retorna a chave para definir o atributo no node')
  test.todo('Atributo value retorna o valor do className do target')
  test.todo('Contrato render.flow define o className se o value for truthy')
  test.todo('Contrato repaint.reflow atualiza o valor se o novo className for diferente')
  test.todo('Metodo estatico create constroi o className')
})
