describe('standard.h.attributes', () => {
  test.todo('Contrato reflow.add adiciona um novo atributo')
  test.todo('Contrato reflow.remove remove um atributo')
  test.todo('Contrato reflow.replace troca um atributo por outro novo atributo')
  test.todo('Contrato render.flow define todos os atributo no target')
  test.todo('Contrato repaint.reflow executa o processo com o novo attributes')
  test.todo('Metodo estatico create mapeia todos os atributos validos')
})
