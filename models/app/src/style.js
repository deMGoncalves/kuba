import styled from '@kuba/styled'

export default styled.style`
  @media (min-width: 769px) {
    .app__main {
      position: absolute;
      right: 0;
      top: 0;
      width: calc(100% - 88px);
    }
  }
`
